[build-system]
requires = ["scikit-build-core", "cython", "cython-cmake @ git+https://github.com/vyasr/cython-cmake@main#subdirectory=cython-cmake"]
build-backend = "scikit_build_core.build"

[project]
name = "example"
version = "0.0.1"

[tool.scikit-build]
wheel.license-files = []
